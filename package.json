{
  "name": "utter-sphere",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/compile-pois.js && astro build",
    "build:prod": "node scripts/compile-pois.js prod && node scripts/prepare-static-build.js && NODE_ENV=production astro build --config astro.config.prod.mjs && node scripts/restore-after-build.js",
    "build:m": "node scripts/compile-pois.js m && node scripts/prepare-static-build.js && NODE_ENV=production astro build --config astro.config.prod.mjs && node scripts/restore-after-build.js",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.12",
    "@astrojs/node": "^9.5.1",
    "@turf/turf": "^7.3.1",
    "astro": "^5.16.4",
    "leaflet": "^1.9.4",
    "proj4": "^2.20.2"
  }
}
